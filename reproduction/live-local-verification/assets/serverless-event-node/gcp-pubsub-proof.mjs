import fs from "node:fs";
import { PubSub } from "@google-cloud/pubsub";
const [projectId, inputTopic, outputTopic, dlqTopic, fixtureRoot, evidenceRoot] = process.argv.slice(2);
const pubsub = new PubSub({ projectId });
const ensureTopic = async (name) => { const topic = pubsub.topic(name); if (!(await topic.exists())[0]) await pubsub.createTopic(name); return topic; };
const input = await ensureTopic(inputTopic), output = await ensureTopic(outputTopic), dlq = await ensureTopic(dlqTopic);
const ensureSub = async (topic, name, options = {}) => { const sub = topic.subscription(name); if (!(await sub.exists())[0]) await topic.createSubscription(name, options); return sub; };
await ensureSub(input, `${inputTopic}-push`, { pushEndpoint: "http://eventarc-bridge:8080/" });
const outputSub = await ensureSub(output, `${outputTopic}-verify`), dlqSub = await ensureSub(dlq, `${dlqTopic}-verify`);
const rows = (name) => fs.readFileSync(`${fixtureRoot}/${name}`, "utf8").trim().split(/\r?\n/).filter(Boolean);
const valid = rows("valid-input.jsonl"), invalid = rows("invalid-input.jsonl");
const transformed = [], errors = [];
const listen = (sub, values) => sub.on("message", (message) => { values.push(JSON.parse(message.data.toString("utf8"))); message.ack(); });
listen(outputSub, transformed); listen(dlqSub, errors);
for (const payload of [...valid, ...invalid]) await input.publishMessage({ data: Buffer.from(payload) });
const started = Date.now(); while ((transformed.length < valid.length || errors.length < invalid.length) && transformed.length + errors.length < valid.length + invalid.length && Date.now() - started < 300000) await new Promise((resolve) => setTimeout(resolve, 1000));
await outputSub.close(); await dlqSub.close();
fs.mkdirSync(`${evidenceRoot}/actual`, { recursive: true }); fs.writeFileSync(`${evidenceRoot}/actual/transformed-output.jsonl`, `${transformed.map(JSON.stringify).join("\n")}\n`); fs.writeFileSync(`${evidenceRoot}/actual/error-output.jsonl`, `${errors.map(JSON.stringify).join("\n")}\n`);
if (transformed.length !== valid.length || errors.length !== invalid.length) throw new Error(`expected ${valid.length}/${invalid.length}, received ${transformed.length}/${errors.length}`);
const result = { trigger: "gcp-pubsub-cloudevent", attemptedInput: transformed.length + errors.length, successfulOutput: transformed.length, errorOutput: errors.length, verifierWriteTargets: [inputTopic], verifierReadTargets: [outputTopic, dlqTopic], envelopeBridge: "local Eventarc-compatible CloudEvent conversion only" };
fs.writeFileSync(`${evidenceRoot}/actual/trigger-result.json`, `${JSON.stringify(result, null, 2)}\n`); console.log(JSON.stringify(result));
