#!/usr/bin/env node
import crypto from "node:crypto";import fs from "node:fs";import path from "node:path";import mqtt from "mqtt";
const[fixtureRoot,bundleRoot,brokerUrl,runId]=process.argv.slice(2);if(![fixtureRoot,bundleRoot,brokerUrl,runId].every(Boolean))throw new Error("usage: emqx-mqtt-raw-only-verifier.mjs <fixtures> <bundle> <broker> <runId>");const topic=(s)=>`flowplane/emqx/${runId.toLowerCase()}/${s}`,topics={raw:topic("raw"),transformed:topic("transformed"),dlq:topic("dlq")};const readText=(f)=>fs.readFileSync(f,"utf8").replace(/^\uFEFF/,""),readJsonl=(f)=>readText(f).split(/\r?\n/).filter(Boolean).map(JSON.parse),valid=readJsonl(path.join(fixtureRoot,"valid-input.jsonl")),invalid=readJsonl(path.join(fixtureRoot,"invalid-input.jsonl")),simulation=JSON.parse(readText(path.join(bundleRoot,"expected","simulation-batch.json"))),invalidSimulation=JSON.parse(readText(path.join(bundleRoot,"expected","simulation-invalid.json"))),expectedById=new Map(simulation.records.map((r)=>[r.recordId,r.outputPreview])),expectedErrorCodes=[...new Set(invalidSimulation.errors.map((e)=>{const m=String(e).match(/^[^:]+:\s+([A-Z0-9_]+)\s+-/);if(!m)throw new Error(`No code: ${e}`);return m[1];}))].sort();const sortJson=(v)=>Array.isArray(v)?v.map(sortJson):v&&typeof v==="object"?Object.fromEntries(Object.keys(v).sort().map((k)=>[k,sortJson(v[k])])):v,canonical=(v)=>JSON.stringify(sortJson(v)),sha256=(v)=>crypto.createHash("sha256").update(v,"utf8").digest("hex");
const client=mqtt.connect(brokerUrl,{clientId:`flowplane-emqx-verifier-${runId}`,clean:true});await new Promise((resolve,reject)=>{client.once("connect",resolve);client.once("error",reject);});const successes=[],errors=[];client.on("message",(name,payload)=>{const value=JSON.parse(payload.toString("utf8"));if(name===topics.transformed)successes.push(value);if(name===topics.dlq)errors.push(value);});await new Promise((resolve,reject)=>client.subscribe({[topics.transformed]:{qos:1},[topics.dlq]:{qos:1}},(error)=>error?reject(error):resolve()));const raw=[...valid.map((payload)=>({payload,recordId:payload.event.id,kind:"valid",runId,publishedBy:"raw-only-verifier"})),...invalid.map((payload)=>({payload,recordId:payload.recordId,kind:"invalid",runId,publishedBy:"raw-only-verifier"}))];const started=Date.now();for(const record of raw)await new Promise((resolve,reject)=>client.publish(topics.raw,JSON.stringify(record),{qos:1},(error)=>error?reject(error):resolve()));const deadline=Date.now()+90000;while((successes.length<100||errors.length<10)&&Date.now()<deadline)await new Promise((r)=>setTimeout(r,100));if(successes.length!==100||errors.length!==10)throw new Error(`MQTT output timeout success=${successes.length} errors=${errors.length}`);let duplicates=0,expectedHashMatches=0,expectedErrorMatches=0;const seen=new Set(),hashes=[];for(const r of successes){if(seen.has(r.recordId))duplicates++;seen.add(r.recordId);const actualHash=sha256(canonical(r.payload)),expectedHash=sha256(canonical(expectedById.get(r.recordId)));if(actualHash===expectedHash)expectedHashMatches++;hashes.push({recordId:r.recordId,expectedHash,actualHash,matched:actualHash===expectedHash});}for(const r of errors){if(seen.has(r.recordId))duplicates++;seen.add(r.recordId);if(canonical([...new Set((r.payload?.errors??[]).map((e)=>e.code))].sort())===canonical(expectedErrorCodes))expectedErrorMatches++;}fs.writeFileSync(path.join(bundleRoot,"actual","transformed-output.jsonl"),`${successes.map(JSON.stringify).join("\n")}\n`);fs.writeFileSync(path.join(bundleRoot,"actual","error-output.jsonl"),`${errors.map(JSON.stringify).join("\n")}\n`);fs.writeFileSync(path.join(bundleRoot,"actual","output-hashes.json"),`${JSON.stringify(hashes,null,2)}\n`);const report={schemaVersion:"flowplane.emqx-mqtt-raw-only-verification-result.v1",runId,topics,verifierWriteTargets:[topics.raw],verifierReadTargets:[topics.transformed,topics.dlq],attemptedInput:110,acceptedInput:110,validInput:100,intentionalInvalid:10,successfulOutput:100,errorOutput:10,filtered:0,duplicates,expectedHashMatches,expectedErrorMatches,expectedErrorCodes,durationSeconds:Math.round((Date.now()-started)/10)/100};fs.writeFileSync(path.join(bundleRoot,"actual","bridge-result.json"),`${JSON.stringify(report,null,2)}\n`);console.log(JSON.stringify(report));await new Promise((resolve)=>client.end(false,resolve));
