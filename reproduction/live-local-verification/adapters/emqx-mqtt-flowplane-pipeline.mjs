#!/usr/bin/env node
import fs from "node:fs";import path from "node:path";import mqtt from "mqtt";
const[brokerUrl,runtimeUrl,runId,expectedText,evidenceRoot]=process.argv.slice(2);if(![brokerUrl,runtimeUrl,runId,expectedText,evidenceRoot].every(Boolean))throw new Error("usage: emqx-mqtt-flowplane-pipeline.mjs <broker> <runtime> <runId> <expected> <evidence>");const expected=Number.parseInt(expectedText,10),topic=(s)=>`flowplane/emqx/${runId.toLowerCase()}/${s}`,topics={raw:topic("raw"),transformed:topic("transformed"),dlq:topic("dlq")};const actualRoot=path.join(evidenceRoot,"actual");fs.mkdirSync(actualRoot,{recursive:true});const writeJson=(n,v)=>fs.writeFileSync(path.join(actualRoot,n),`${JSON.stringify(v,null,2)}\n`);let successfulOutput=0,errorOutput=0,unexpectedFailures=0,httpTimeouts=0,connectionErrors=0;const httpStatusCounts={};let sequence=0;let completeResolve,completeReject;const complete=new Promise((resolve,reject)=>{completeResolve=resolve;completeReject=reject;});
const publishConfirmed=(client,target,value)=>new Promise((resolve,reject)=>client.publish(target,JSON.stringify(value),{qos:1,retain:false},(error)=>error?reject(error):resolve()));
const publisher=mqtt.connect(brokerUrl,{clientId:`flowplane-emqx-publisher-${runId}`,protocolVersion:5,clean:true,reconnectPeriod:1000});
const client=mqtt.connect(brokerUrl,{clientId:`flowplane-emqx-pipeline-${runId}`,protocolVersion:5,clean:false,reconnectPeriod:1000,customHandleAcks:(receivedTopic,message,packet,done)=>{(async()=>{if(receivedTopic!==topics.raw){done(0);return;}const index=sequence++;const envelope=JSON.parse(message.toString("utf8")),payload=envelope.payload,recordId=envelope.recordId??payload?.recordId??payload?.event?.id;let response;try{response=await fetch(runtimeUrl,{method:"POST",headers:{"content-type":"application/json","x-flowplane-source-topic":topics.raw,"x-flowplane-source-partition":"0","x-flowplane-source-offset":String(index),"x-flowplane-source-key":recordId},body:JSON.stringify(payload),signal:AbortSignal.timeout(30000)});}catch(error){if(error?.name==="TimeoutError")httpTimeouts++;else connectionErrors++;throw error;}httpStatusCounts[response.status]=(httpStatusCounts[response.status]??0)+1;const bodyText=await response.text(),body=JSON.parse(bodyText);if(response.status===200){await publishConfirmed(publisher,topics.transformed,{recordId,runId,payload:body,publishedBy:"emqx-mqtt-flowplane-pipeline"});successfulOutput++;}else if(response.status===422){await publishConfirmed(publisher,topics.dlq,{recordId,runId,payload:body,publishedBy:"emqx-mqtt-flowplane-pipeline"});errorOutput++;}else{unexpectedFailures++;throw new Error(`Unexpected Flowplane HTTP ${response.status}: ${bodyText.slice(0,500)}`);}done(0);if(successfulOutput+errorOutput===expected)completeResolve();})().catch((error)=>{done(128);completeReject(error);});}});
await Promise.all([new Promise((resolve,reject)=>{publisher.once("connect",resolve);publisher.once("error",reject);}),new Promise((resolve,reject)=>{client.once("connect",resolve);client.once("error",reject);})]);await new Promise((resolve,reject)=>client.subscribe(topics.raw,{qos:1},(error)=>error?reject(error):resolve()));writeJson("pipeline-ready.json",{schemaVersion:"flowplane.emqx-mqtt-pipeline-ready.v1",runId,ready:true,readTargets:[topics.raw],writeTargets:[topics.transformed,topics.dlq],qos:1,deferredPubAck:true,separateConfirmedPublisher:true,runtimeUrl,readyAt:new Date().toISOString()});console.log(JSON.stringify({event:"pipeline-ready",topics}));const started=Date.now();await complete;const result={schemaVersion:"flowplane.emqx-mqtt-pipeline-result.v1",runId,component:"independent EMQX MQTT-to-Flowplane pipeline",boundary:"EMQX raw QoS1 subscription -> Flowplane HTTP -> separate-client confirmed EMQX transformed/DLQ QoS1 publish -> raw PUBACK",readTargets:[topics.raw],writeTargets:[topics.transformed,topics.dlq],processedInput:successfulOutput+errorOutput,successfulOutput,errorOutput,unexpectedFailures,httpTimeouts,connectionErrors,httpStatusCounts,durationSeconds:Math.round((Date.now()-started)/10)/100};writeJson("pipeline-result.json",result);console.log(JSON.stringify(result));await Promise.all([new Promise((resolve)=>client.end(false,resolve)),new Promise((resolve)=>publisher.end(false,resolve))]);
