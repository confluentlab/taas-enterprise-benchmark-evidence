#!/usr/bin/env node
import fs from "node:fs";
import path from "node:path";
import amqp from "amqplib";

const [amqpUrl,runtimeUrl,runId,expectedText,evidenceRoot]=process.argv.slice(2);
if(![amqpUrl,runtimeUrl,runId,expectedText,evidenceRoot].every(Boolean))throw new Error("usage: rabbitmq-streams-flowplane-pipeline.mjs <amqpUrl> <runtimeUrl> <runId> <expected> <evidenceRoot>");
const expected=Number.parseInt(expectedText,10);const queue=(suffix)=>`flowplane.rabbitmq.${runId.toLowerCase()}.${suffix}`;const queues={raw:queue("raw"),transformed:queue("transformed"),dlq:queue("dlq")};
const actualRoot=path.join(evidenceRoot,"actual");fs.mkdirSync(actualRoot,{recursive:true});const writeJson=(name,value)=>fs.writeFileSync(path.join(actualRoot,name),`${JSON.stringify(value,null,2)}\n`);
const connection=await amqp.connect(amqpUrl);const channel=await connection.createConfirmChannel();await channel.prefetch(1);
for(const name of Object.values(queues))await channel.assertQueue(name,{durable:true,arguments:{"x-queue-type":"stream","x-max-age":"1h"}});
writeJson("pipeline-ready.json",{schemaVersion:"flowplane.rabbitmq-streams-pipeline-ready.v1",runId,ready:true,readTargets:[queues.raw],writeTargets:[queues.transformed,queues.dlq],runtimeUrl,readyAt:new Date().toISOString()});console.log(JSON.stringify({event:"pipeline-ready",runId,queues}));
const buffered=[];const waiting=[];await channel.consume(queues.raw,(message)=>{const waiter=waiting.shift();if(waiter)waiter(message);else buffered.push(message);},{noAck:false,arguments:{"x-stream-offset":"first"}});
const nextMessage=()=>buffered.length?Promise.resolve(buffered.shift()):new Promise((resolve)=>waiting.push(resolve));
let successfulOutput=0,errorOutput=0,unexpectedFailures=0,httpTimeouts=0,connectionErrors=0;const httpStatusCounts={};const started=Date.now();
for(let index=0;index<expected;index+=1){const message=await nextMessage();const envelope=JSON.parse(message.content.toString("utf8"));const payload=envelope.payload;const recordId=envelope.recordId??payload?.recordId??payload?.event?.id;let response;try{response=await fetch(runtimeUrl,{method:"POST",headers:{"content-type":"application/json","x-flowplane-source-topic":queues.raw,"x-flowplane-source-partition":"0","x-flowplane-source-offset":String(index),"x-flowplane-source-key":recordId},body:JSON.stringify(payload),signal:AbortSignal.timeout(30000)});}catch(error){if(error?.name==="TimeoutError")httpTimeouts+=1;else connectionErrors+=1;throw error;}httpStatusCounts[response.status]=(httpStatusCounts[response.status]??0)+1;const bodyText=await response.text();const body=JSON.parse(bodyText);let target;if(response.status===200){target=queues.transformed;successfulOutput+=1;}else if(response.status===422){target=queues.dlq;errorOutput+=1;}else{unexpectedFailures+=1;throw new Error(`Unexpected Flowplane HTTP ${response.status}: ${bodyText.slice(0,500)}`);}channel.sendToQueue(target,Buffer.from(JSON.stringify({recordId,runId,payload:body,publishedBy:"rabbitmq-streams-flowplane-pipeline"})),{persistent:true,contentType:"application/json"});await channel.waitForConfirms();channel.ack(message);}
const result={schemaVersion:"flowplane.rabbitmq-streams-pipeline-result.v1",runId,component:"independently deployed RabbitMQ Streams-to-Flowplane pipeline",boundary:"RabbitMQ raw stream consumer -> Flowplane HTTP -> confirmed RabbitMQ transformed/DLQ stream publish",readTargets:[queues.raw],writeTargets:[queues.transformed,queues.dlq],processedInput:successfulOutput+errorOutput,successfulOutput,errorOutput,unexpectedFailures,httpTimeouts,connectionErrors,httpStatusCounts,durationSeconds:Math.round((Date.now()-started)/10)/100};writeJson("pipeline-result.json",result);console.log(JSON.stringify(result));await channel.close();await connection.close();
