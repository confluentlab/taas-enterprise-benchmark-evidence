#!/usr/bin/env node
import crypto from "node:crypto";import fs from "node:fs";import path from "node:path";import amqp from "amqplib";
const[fixtureRoot,bundleRoot,amqpUrl,runId]=process.argv.slice(2);if(![fixtureRoot,bundleRoot,amqpUrl,runId].every(Boolean))throw new Error("usage: rabbitmq-streams-raw-only-verifier.mjs <fixtures> <bundle> <amqpUrl> <runId>");const queue=(suffix)=>`flowplane.rabbitmq.${runId.toLowerCase()}.${suffix}`;const queues={raw:queue("raw"),transformed:queue("transformed"),dlq:queue("dlq")};const readText=(f)=>fs.readFileSync(f,"utf8").replace(/^\uFEFF/,"");const readJsonl=(f)=>readText(f).split(/\r?\n/).filter(Boolean).map(JSON.parse);const valid=readJsonl(path.join(fixtureRoot,"valid-input.jsonl")),invalid=readJsonl(path.join(fixtureRoot,"invalid-input.jsonl"));const simulation=JSON.parse(readText(path.join(bundleRoot,"expected","simulation-batch.json"))),invalidSimulation=JSON.parse(readText(path.join(bundleRoot,"expected","simulation-invalid.json")));const expectedById=new Map(simulation.records.map((r)=>[r.recordId,r.outputPreview]));const expectedErrorCodes=[...new Set(invalidSimulation.errors.map((e)=>{const m=String(e).match(/^[^:]+:\s+([A-Z0-9_]+)\s+-/);if(!m)throw new Error(`Cannot extract code: ${e}`);return m[1];}))].sort();const sortJson=(v)=>Array.isArray(v)?v.map(sortJson):v&&typeof v==="object"?Object.fromEntries(Object.keys(v).sort().map((k)=>[k,sortJson(v[k])])):v;const canonical=(v)=>JSON.stringify(sortJson(v)),sha256=(v)=>crypto.createHash("sha256").update(v,"utf8").digest("hex");
const connection=await amqp.connect(amqpUrl),channel=await connection.createConfirmChannel();await channel.prefetch(100);for(const name of Object.values(queues))await channel.assertQueue(name,{durable:true,arguments:{"x-queue-type":"stream","x-max-age":"1h"}});const raw=[...valid.map((payload)=>({payload,recordId:payload.event.id,kind:"valid",runId,publishedBy:"raw-only-verifier"})),...invalid.map((payload)=>({payload,recordId:payload.recordId,kind:"invalid",runId,publishedBy:"raw-only-verifier"}))];const started=Date.now();for(const record of raw)channel.sendToQueue(queues.raw,Buffer.from(JSON.stringify(record)),{persistent:true,contentType:"application/json"});await channel.waitForConfirms();
const receive=async(name,count)=>{const rows=[],buffered=[],waiting=[];const{consumerTag}=await channel.consume(name,(m)=>{const w=waiting.shift();if(w)w(m);else buffered.push(m);},{noAck:false,arguments:{"x-stream-offset":"first"}});for(let i=0;i<count;i++){const m=buffered.length?buffered.shift():await new Promise((resolve)=>waiting.push(resolve));rows.push(JSON.parse(m.content.toString("utf8")));channel.ack(m);}await channel.cancel(consumerTag);return rows;};const successes=await receive(queues.transformed,valid.length),errors=await receive(queues.dlq,invalid.length);const seen=new Set();let duplicates=0,expectedHashMatches=0;const hashes=[];for(const r of successes){if(seen.has(r.recordId))duplicates++;seen.add(r.recordId);const actualHash=sha256(canonical(r.payload)),expectedHash=sha256(canonical(expectedById.get(r.recordId)));if(actualHash===expectedHash)expectedHashMatches++;hashes.push({recordId:r.recordId,expectedHash,actualHash,matched:actualHash===expectedHash});}let expectedErrorMatches=0;for(const r of errors){if(seen.has(r.recordId))duplicates++;seen.add(r.recordId);const codes=[...new Set((r.payload?.errors??[]).map((e)=>e.code))].sort();if(canonical(codes)===canonical(expectedErrorCodes))expectedErrorMatches++;}fs.writeFileSync(path.join(bundleRoot,"actual","transformed-output.jsonl"),`${successes.map(JSON.stringify).join("\n")}\n`);fs.writeFileSync(path.join(bundleRoot,"actual","error-output.jsonl"),`${errors.map(JSON.stringify).join("\n")}\n`);fs.writeFileSync(path.join(bundleRoot,"actual","output-hashes.json"),`${JSON.stringify(hashes,null,2)}\n`);const report={schemaVersion:"flowplane.rabbitmq-streams-raw-only-verification-result.v1",runId,queues,verifierWriteTargets:[queues.raw],verifierReadTargets:[queues.transformed,queues.dlq],attemptedInput:raw.length,acceptedInput:raw.length,validInput:valid.length,intentionalInvalid:invalid.length,successfulOutput:successes.length,errorOutput:errors.length,filtered:0,duplicates,expectedHashMatches,expectedErrorMatches,expectedErrorCodes,durationSeconds:Math.round((Date.now()-started)/10)/100};fs.writeFileSync(path.join(bundleRoot,"actual","bridge-result.json"),`${JSON.stringify(report,null,2)}\n`);console.log(JSON.stringify(report));await channel.close();await connection.close();
